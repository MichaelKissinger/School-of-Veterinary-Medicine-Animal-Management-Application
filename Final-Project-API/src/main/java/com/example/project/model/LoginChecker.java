package com.example.project.Model;

import java.util.ArrayList;

public class LoginChecker {
    private ArrayList<Object> loginDB;


    public LoginChecker(ArrayList<Object> database) {
        loginDB = database;
    }

    public void addNewUser(String userName, int pass) {
//        loginDB.add(new User(userName, pass));
    }

//    public boolean checkUsername(String user) {
//        for (String userName : loginDB.getName()) {
//            if (userName == user) ;
//            return true;
//        }
//        return false;
//    }

//    public boolean checkPassword(int pass) {
//        for (int password : loginDB.getPass()) {
//            if (password == pass) ;
//            return true;
//        }
//        return false;
//    }
}
